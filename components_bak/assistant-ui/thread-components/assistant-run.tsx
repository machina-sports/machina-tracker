'use client';

import { memo } from 'react';
import type { FC } from 'react';

const AssistantRun: FC = () => {
  // Componente vazio para substituir o loading padrão do assistant-ui
  // O loading customizado está sendo renderizado em ThreadLoading
  return null;
};

export default memo(AssistantRun);
